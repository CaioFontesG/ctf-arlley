# ─── Flags ───────────────────────────────────────────────
FLAG_RECONHECIMENTO=FLAG{...}
FLAG_ENUMERACAO=FLAG{...}
FLAG_CREDENCIAIS=FLAG{...}
FLAG_VALIDACAO_FRONT=FLAG{...}
FLAG_DEVTOOLS=FLAG{...}
FLAG_HEADER=FLAG{...}
FLAG_FUZZING=FLAG{...}
FLAG_LFI=FLAG{...}
FLAG_SQL_INJECTION=FLAG{...}
FLAG_UPLOAD=FLAG{...}
FLAG_CADEIA=FLAG{...}

# ─── CTFd ────────────────────────────────────────────────
CTF_NAME=CTF Arlley
CTF_DESCRIPTION=Desafios introdutórios de segurança web
USER_MODE=users

# ─── Admin ───────────────────────────────────────────────
ADMIN_NAME=admin
ADMIN_EMAIL=admin@ctf.local
ADMIN_PASSWORD=troque_isso

# ─── Infra ───────────────────────────────────────────────
# IP público ou domínio da VPS
CHALLENGE_BASE_URL=http://SEU_IP_OU_DOMINIO

# Gere com: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=troque_isso
